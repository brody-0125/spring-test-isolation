pluginManagement {
    plugins {
        id 'com.vanniktech.maven.publish' version '0.30.0'
    }
    includeBuild('plugin')
    repositories { mavenLocal(); gradlePluginPortal(); mavenCentral() }
}
rootProject.name = 'spring-test-isolation'
include 'descriptor', 'runtime', 'maven-plugin', 'verification', 'verification-peer'

