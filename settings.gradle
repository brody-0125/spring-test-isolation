pluginManagement {
    plugins {
        id 'com.vanniktech.maven.publish' version '0.30.0'
    }
    repositories { gradlePluginPortal(); mavenCentral() }
}
rootProject.name = 'spring-test-isolation'
include 'runtime', 'maven-plugin', 'plugin', 'verification', 'verification-peer'
