# Verification status

On 2026-09-17, we ran the Docker tests with the versions below under the stated adapter contracts. For negative scenarios, the verification scripts required a Gradle failure and checked that forbidden test bodies did not execute.

Verified environment: Amazon Corretto 17.0.9, Gradle 8.14.3, Spring Boot 3.5.1 / Spring 6.2.8, JUnit 5.12.2, Smart Context 1.0, Testcontainers 2.0.5, PostgreSQL 16.9-alpine, and Redis 7.4.4-alpine. Windows host with Docker Desktop Linux engine 29.7.2, Ryzen 5 5600X, 12 logical processors, and 31.9 GiB RAM.

| Criterion | Status and evidence |
|---|---|
| C1 Parallel workers / sequential execution within each worker | PASS: `scripts/verify.ps1` compares execution intervals for six classes, checking overlap across workers and no overlap within a worker |
| C2 Storage and Pub/Sub isolation | PASS: identical primary keys and Redis keys, real HTTP writes, namespaced pattern subscriptions, and FLUSHALL rejected with NOPERM |
| C3/C4 Context reuse / side-effect-free configuration discovery | PASS: context identity reused within a worker; DiscoveryTest runs without a descriptor |
| C5/C6 Separate context and storage lifecycles | PASS: configuration groups run in sequence; cleanup removes destruction callback writes; two modules share one container startup and shutdown |
| C7/C8 Delayed work, destruction callbacks, and cleanup failures | PASS: delayed writes, local state, and destruction callback rows do not leak into the next class; a failure or 500 ms timeout prevents the next class's body from running |
| C9 Reject conflicting configuration | PASS: guards reject concurrent execution annotations, missing required listeners, connections to another database, and disabled extension autodetection |
| C10 Failures, retries, and worker crashes | PASS: initialization failure blocks test bodies; after halt(17) in a separate JVM, a new JVM receives different DB/Redis storage; Redis allocation failure after DB creation removes the DB; multiple modules verified |
| Sequential versus parallel time, memory, connection, and thread measurements | PASS: measurements completed under the conditions below; results apply to these fixtures |

## Execution evidence

The paths below are local build outputs. Git excludes `build/` directories, so a fresh clone will not contain these logs or reports. Run the verification scripts to generate them. Earlier baseline runs used the prototype's previous project name; their results remain historical measurements rather than fresh runs of the renamed project.

## Verification after the repository rename

On 2026-09-17, we reran these checks with the `spring-test-isolation` build names, plugin ID, Maven coordinates, Java packages, and system properties:

| Check | Result | Local log |
|---|---|---|
| Runtime unit tests, plugin validation, and POM generation | PASS | `build/evidence/rename-build.log` |
| Full suite with 2 workers | PASS: 2 runtime and 11 integration tests | `build/evidence/workers-2-negative-False.log` |
| Workflow barriers with 2 workers | PASS: 4 tests | `build/evidence/workflow-2-run-1.log` |
| Two modules sharing containers | PASS: 22 tests | `build/evidence/multimodule.log` |
| Failure contracts | PASS: 7 expected failures | Logs generated by `scripts/verify-failures.ps1` |
| Publication to an isolated local Maven repository | PASS: plugin marker, plugin, and runtime artifacts | `build/evidence/consumer-publication.log` |
| Separate consumer using the README dependency example | PASS: plugin discovery, Spring/JUnit registration, JDBC database selection, and Redis read/write | `build/evidence/consumer-smoke.log` |

The consumer used Maven artifact resolution rather than the source build's included plugin build or project dependencies. This validates the renamed publication path. The earlier 1/4-worker runs and memory baseline below predate the rename; we did not repeat those measurements during repository preparation.

## Earlier runs and log inventory

- `build/evidence/workers-1-negative-False.log`, `workers-2-negative-False.log`: 2 runtime unit tests and 11 integration/worker recovery tests (7 existing tests plus 4 complex workflow tests).
- `build/evidence/workers-1-events.json`, `workers-2-events.json`: execution intervals and context identity for each class.
- `build/evidence/multimodule.log`: 22 tests across two modules and one infrastructure-start/closed lifecycle.
- `build/evidence/workers-1-negative-True.log`: cleanup fails in the first class; the second class's body does not execute.
- `build/evidence/boundaryTimeoutTest.log`: the second class's body does not execute after a quiesce hook timeout.
- `build/evidence/guardFailureTest.log`, `missingListenerTest.log`, `connectionGuardTest.log`, `initializationFailureTest.log`, `invalidSettingsTest.log`: expected failures for each conflict or error.
- `build/evidence/filtered-smoke.log`: a single selected class runs against the final binaries, with correlated run/task/worker/namespace diagnostics and normal shutdown.
- `build/evidence/final-assembly.log`, `publication-check.log`: successful JAR assembly, Gradle validatePlugins, and publication POM generation. We did not upload artifacts to an external repository.
- Each module's `build/test-results` and `build/reports/tests`: JUnit XML/HTML from the last execution of that task. Each run replaces these reports; the logs above preserve earlier runs. Negative fixtures produce failed test reports by design. Use the verification script's verdict to check whether the failure matched expectations.

## Integration verification across connected resources

The fixtures in `verification/src/test/java/workflow` exercise this path:

HTTP → Redis lock → PostgreSQL orders, items with foreign keys, and outbox → Redis queue → asynchronous SQL projection → Redis cache and Pub/Sub.

| Execution | Checks | Result |
|---|---|---|
| `./scripts/verify-workflow.ps1 -Workers 1` | Four sequential classes, context reuse, identity reset, and cleanup of delayed work and destruction writes | PASS / 4 tests |
| `./scripts/verify-workflow.ps1 -Workers 2 -Runs 3` | Barrier-enforced concurrency, preservation of another worker's data during cleanup, and context reuse | PASS / 4 tests × 3 runs |
| `./scripts/verify-workflow.ps1 -Workers 4` | Four JVMs use the same primary keys, Redis keys, and logical channel names at the same time; each observes its own data and messages | PASS / 4 tests |
| `./scripts/verify.ps1 -Workers 1` and `-Workers 2` | Existing scenarios, worker recovery, and the new workflow in the same execution | PASS / 2 runtime + 11 integration tests per run |
| `./scripts/verify-multimodule.ps1` | Two modules share one container pair, including the workflow fixtures | PASS / 22 tests |
| `./scripts/verify-failures.ps1` | Cleanup failure, timeout, concurrent execution annotation, missing listener, wrong database, initialization failure, and configuration conflict | PASS / 7 expected failure scenarios |

Each tested workflow configuration covers DB rollback, retransmission after an enqueue failure, cache recovery after a SQL projection commit failure, and duplicate processing prevention. A listener after cleanup checks that the five SQL tables and the Redis database contain no residual data, including after the final class. The scripts require successful `close()` completion and confirm that Docker has removed the containers. We ran 20 dedicated workflow scenarios; that sample is too small to establish statistical reliability.

Evidence: `build/evidence/workflow-1-run-1.log`, `workflow-2-run-{1,2,3}.log`, `workflow-4-run-1.log`, and the corresponding `*-events.json` files. The development log `workflow-first.log` records a duplicate controller registration failure that we fixed before the final runs.

We counted four workflow completions and four cleanup audits in each single-module log, eight of each in the two-module log, and one infrastructure startup and shutdown per positive run. Final positive logs contain no Spring close/destroy failure or worker storage cleanup failure warnings. The workflow expansion left runtime/plugin behavior unchanged.

An early run passed despite a bean destruction exception that Spring logged as a warning. We changed the fixture to use a worker-owned connection for its deliberate Redis writes during destruction and made the verification script fail on those warnings. That check belongs to the verification script; the runtime does not collect arbitrary Spring destruction exceptions.

## Resource measurement baseline for the original fixtures

We measured the original 7 integration/worker recovery tests before adding the workflow fixtures. The expanded set has 11 tests and requires a new measurement. We ran one worker, then two workers, with one measurement per configuration, cached images and dependencies, and no other verification builds in progress.

These runs include --rerun-tasks compilation, Gradle, container startup and shutdown, unit tests, and JVM crash/retry probes. The six original Spring fixtures each include about 1.2 seconds of deliberate waiting. Use these figures as a fixture baseline, without extrapolating to application performance.

| Metric | 1 worker | 2 workers |
|---|---:|---:|
| Total script wall-clock time | 25.63 s | 22.12 s |
| Maximum sampled aggregate Working Set of Spring test JVMs | 289.5 MiB | 580.2 MiB |
| Maximum sampled aggregate Private Bytes of the same JVMs | 324.1 MiB | 658.6 MiB |
| Memory sample count | 51 | 42 |
| Total contexts created | 2 | 3 |
| Maximum observed active contexts per worker | 1 | 1 |
| Maximum JVM thread count sampled at test start, per worker | 41 | 41 |
| Maximum DB connection count sampled at test start, per worker | 1 | 1 |

We sampled Windows process memory at intervals of about 250 ms. Aggregate Working Set can double-count shared pages and miss peaks between samples. The totals exclude the Gradle daemon, Docker/DB servers, and short-lived crash/retry probe JVMs. We sampled thread and DB connection counts at test start, so those counts can miss later peaks.

The files `workers-*-memory.json` retain the historical memory samples. The expanded verification replaced `workers-*-negative-False.log` and `workers-*-events.json`; those files now describe different runs.

## Shared infrastructure descriptor

Library, Testcontainers, and container image versions are defined in [gradle/dependency-versions.gradle](gradle/dependency-versions.gradle). The plugin SPI reads Docker image tags from `InfrastructureVersions` (generated at compile time from that file). Runtime and verification modules resolve Maven coordinates from the same `versions` map.

The Gradle Build Service writes a `connection.properties` file per build and passes its path to workers as `-Dspringtestisolation.descriptor=…`. The runtime reads backend ids and connection endpoints from this file; workers never share slot files within a build.

| Property | Meaning |
|----------|---------|
| `jdbc.backend` | JDBC isolation implementation (`postgresql` default). Registered in `JdbcWorkerBackends` / `JdbcInfrastructureProvider`. |
| `cache.backend` | Cache isolation implementation (`redis` default). Registered in `CacheWorkerBackends` / `CacheInfrastructureProvider`. |
| `jdbc` | Admin JDBC URL for creating and dropping per-worker databases (PostgreSQL today). |
| `user`, `password` | Credentials for admin and worker JDBC connections. |
| `redis.host`, `redis.port` | Shared cache server endpoint. |
| `slots` | Maximum concurrent cache logical databases (file-backed slot reservation under the descriptor directory). |
| `run` | Build-scoped run id (diagnostics). |

Gradle extension (defaults preserve 1.0.0 behavior):

```groovy
isolatedTests {
    workers = 2
    jdbcBackend = 'postgresql'
    cacheBackend = 'redis'
}
```

Optional consumer overrides (unset = plugin defaults from `dependency-versions.gradle` / `InfrastructureVersions`):

| DSL property | Applies when | Default |
|--------------|--------------|---------|
| `postgresImage` | `jdbcBackend = 'postgresql'` | `postgres:16.9-alpine` |
| `mysqlImage` | `jdbcBackend = 'mysql'` | `mysql:8.4.5` |
| `redisImage` | `cacheBackend = 'redis'` | `redis:7.4.4-alpine` |
| `redisLogicalDatabases` | `cacheBackend = 'redis'` | `256` |
| `maxCacheSlots` | `cacheBackend = 'redis'` | `255` |

Changing images or Redis capacity is **not** part of the verified matrix unless you record new evidence. Overrides must keep `maxCacheSlots ≤ redisLogicalDatabases` and `workers ≤ maxCacheSlots`.

Additional engines register by implementing `JdbcWorkerBackend` / `CacheWorkerBackend` in the runtime module and `JdbcInfrastructureProvider` / `CacheInfrastructureProvider` in the plugin module, then wiring ids into the registries.

One Gradle build uses a single shared Build Service and one `jdbc.backend` / `cache.backend` pair; the first registered plugin configuration wins if subprojects disagree.

### JDBC backend evidence

| Backend | Container image | Verification command | Evidence log |
|---------|-----------------|----------------------|--------------|
| `postgresql` (default) | `postgres:16.9-alpine` | `./scripts/verify.ps1` | `build/evidence/workers-*-negative-False.log` |
| `mysql` | `mysql:8.4.5` | `./scripts/verify-mysql.ps1` | `build/evidence/mysql-workers-*.log` |

MySQL coverage includes per-worker database create/drop, `WorkerStore.reset()`, Flyway smoke, storage reset smoke, and the connection guard negative fixture. Workflow fixtures remain PostgreSQL-only.

## Remaining limitations

- We tested the version combination above under the ClassBoundary and storage access contracts. Test other versions, operating systems, and large applications before adopting them.
- More workers increase JVM/context memory use. You must size the worker count and heaps to avoid OOM.
- Your ClassBoundary quiesce/reset/resume implementation must track and stop application threads, schedulers, and external clients. The runtime does not track that work for you.
- Redis ACLs block FLUSHALL and channels outside the namespace. SELECT can still choose another logical database, so use trusted tests and the supported access paths.
- Spring Session's Redis keyevent channels need integration beyond the current namespace contract. These fixtures cover application channels. We did not run Spring Session or Spring Modulith compatibility tests.
- We have not verified or claimed support for Redis Cluster, Kotest, `@ContextHierarchy`, `@Nested` classes that declare their own context, configuration cache, or remote workers in distributed CI. `@Nested` inherit-only support is covered by `DiscoveryTest` and `NestedInheritTest`; historical 2026-09-17 logs predate that fixture.
- TestNG native (`useTestNG()`, TestNG 7.10.2): Gradle rejects `parallel`, `threadCount` > 1, and suite XML. Smart Context `SmartDirtiesSuiteListener` orders classes. Kotest can use JUnit Platform and Smart Context AutoScan, but this repository has no Kotest fixture (C10), so Kotest stays unsupported. The plugin still forces `kotest.framework.parallelism=1` on JUnit Platform tasks.
- We provide source code, the Gradle Wrapper, local publication definitions, and runnable fixtures. We have not published artifacts to a public registry.
- We verified retry by allocating fresh storage in a new JVM. Test external retry plugins that repeat a JUnit execution plan within the same JVM as a separate case.
